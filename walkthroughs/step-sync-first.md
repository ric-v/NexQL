# First sync

After setup, sync runs automatically. Watch the status bar:

- **Sync ✓** — up to date
- **Sync paused** — offline, retrying with backoff
- **Sync (n)** — conflicts need review

Click the status bar for **Sync Now**, **Pause**, or **Sign Out**.
