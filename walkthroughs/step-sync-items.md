# Choose sync items

During setup, choose what to sync:

- **Connections** — host, port, username (passwords opt-in)
- **Saved queries** — your query library
- **Notebooks** — `.pgsql` cell content (outputs excluded)

Machine-local SSL/SSH paths stay on each device.
