{
  "name": "nexql-ext-sqlite",
  "displayName": "NexQL - SQLite",
  "version": "1.0.0",
  "description": "SQLite database support for NexQL",
  "publisher": "ric-v",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dev-asterix/PgStudio.git",
    "directory": "packages/ext-sqlite"
  },
  "engines": {
    "vscode": "^1.105.0",
    "node": ">=18.0.0"
  },
  "extensionDependencies": [
    "ric-v.nexql"
  ],
  "categories": [
    "Other"
  ],
  "keywords": [
    "sqlite",
    "sqlite3",
    "database",
    "sql",
    "nexql",
    "embedded database",
    "db client"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  },
  "scripts": {
    "vscode:prepublish": "npm run esbuild-base -- --minify",
    "build": "npm run compile",
    "compile": "npm run esbuild-base",
    "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --external:better-sqlite3 --format=cjs --platform=node --main-fields=main --alias:@nexql/core=../core/src",
    "watch": "npm run esbuild-base -- --sourcemap --watch",
    "typecheck": "tsc -p ./ --noEmit",
    "package": "npx @vscode/vsce package",
    "package:nightly": "npx @vscode/vsce package --pre-release",
    "publish:vsce": "npx @vscode/vsce publish -p $VSCE_PAT",
    "publish:ovsx": "npx ovsx publish -p $OVSX_PAT",
    "test": "echo 'no tests yet' && exit 0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.5.1",
    "@types/vscode": "^1.105.0",
    "esbuild": ">=0.28.0",
    "typescript": "^6.0.2"
  }
}
