/**
 * SQL-specific abstractions for relational database engines.
 * Re-exports from their canonical locations for category-based access.
 */
export type { DbDialect } from '../../db/DbDialect';
export type { SqlTemplateProvider } from '../../db/SqlTemplateProvider';
export type { IntrospectionProvider } from '../../db/introspection/IntrospectionProvider';
