/** Default number of histogram buckets (matches ColumnProfilePanel convention). */
export const HISTOGRAM_BUCKET_COUNT = 10;

/** Max distinct values per pivot dimension (row or column axis). */
export const MAX_PIVOT_DISTINCT = 40;

/** Cap for distinct counting in summary stats (performance guard). */
export const DISTINCT_COUNT_CAP = 10001;
